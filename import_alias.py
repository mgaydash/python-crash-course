from add import add as add_nums
# Can also alias a module: import add as math << renames 'add' to 'math'j

# Can import all functions with 'from add import *' Then functions
# can be called without referencing the module name

print(add_nums(3, 7))